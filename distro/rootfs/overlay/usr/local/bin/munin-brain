#!/usr/bin/env bash
set -euo pipefail

BIN="/opt/muninos/bin/munin-brain"
if [[ -x "$BIN" ]]; then
  exec "$BIN" serve --listen 0.0.0.0:8790
fi

echo "[munin-brain] missing native binary: $BIN" >&2
exit 1
