#!/usr/bin/env bash
set -euo pipefail

BIN="/opt/muninos/bin/munin-audio"
if [[ -x "$BIN" ]]; then
  exec "$BIN" start --sample-rate 16000 --frame-ms 20 --brain-endpoint http://127.0.0.1:8790
fi

echo "[munin-audio] missing native binary: $BIN" >&2
exit 1
